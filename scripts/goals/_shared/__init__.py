"""Reusable helpers for /goal-driven backtest campaigns.

Each campaign lives in `scripts/goals/<date>_<strategy>_<symbol>/`.
Common helpers (harness, engine settings, preset I/O, trade analysis)
live here so campaigns stay slim and consistent across runs.
"""
